import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#28A745",
    },
    secondary: {
      main: "#28A745",
    },
  },
  typography: {
    fontFamily: `"Poppins"`,
  },
});

export default theme;
